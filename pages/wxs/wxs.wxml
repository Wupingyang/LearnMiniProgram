<!--1.wxs的定义方式-->
<!--1.1直接在wxml文件中定义-->
<!-- <wxs module='info'>
  // JS代码
  var msg = "Hello World"
  var name = "WittyWU"
  var sum = function(num1, num2){
    return num1 + num2
  }

  //commonjs的模块化导出的写法
  module.exports = {
    msg: msg,
    name: name,
    sum: sum
  }
</wxs>
-->

<!--1.2定义在单独的wxs文件中，再使用<wxs>标签进行导入-->
<!--不能使用绝对路径，只能使用相对路径-->
<wxs src='../../wxs/info.wxs' module='info'/>
<view>{{info.msg}}</view>
<view>{{info.name}}</view>
<view>{{info.sum(20, 30)}}</view>

<!--2.wxs的应用-->
<!--2.1小数点后四舍五入保留位数-->
<wxs src='../../wxs/format.wxs' module='format'/>
<view>{{format.priceFormat(price, 3)}}</view>
<view>{{format.dateFormat(time, 'yyyy-MM-dd hh:mm:ss')}}</view>